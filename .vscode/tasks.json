{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "jar",
            "type": "process",
            "command": "jar cf ${workspaceRoot}/dist/jar-file ${workspaceRoot}/build/classes"
        }
    ]
}